import 'package:flutter/material.dart';

class NewMetricForm extends StatelessWidget {
  const NewMetricForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("New Metic Form"),
    );
  }
}
